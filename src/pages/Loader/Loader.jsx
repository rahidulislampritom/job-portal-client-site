const Loader = () => {
    return (
        <div className="min-h-screen flex justify-center items-center">
            <span className="loading loading-spinner text-warning size-18"></span>
        </div>
    );
};

export default Loader;